name: Bonjour
on:
  - push
jobs:
  dire_bonjour:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v5   
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: node -v
      - run: npm ci
      - run: npm run build
